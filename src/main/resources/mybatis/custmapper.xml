<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kbstar.mapper.CustMapper"> <!-- 매퍼가 binding 하겠다. -->
<!--<mapper namespace="com.kbstar.frame.KBMapper">-->
    <select id="select" parameterType="String" resultType="cust"> <!-- id는 함수명 -->
        SELECT * FROM cust WHERE ID=#{id}
    </select>
    <select id="selectall" resultType="cust">
        SELECT * FROM cust
    </select>
    <insert id="insert" parameterType="cust"> <!-- cust는 대소문자 가리지 않는다 -->
        INSERT INTO cust VALUES (#{id},#{pwd}, #{name})
    </insert>
    <update id="update" parameterType="cust">
        UPDATE cust SET NAME=#{name},PWD=#{pwd} WHERE ID=#{id}
    </update>
    <delete id="delete" parameterType="String">
        DELETE FROM cust WHERE ID=#{id}
    </delete>

</mapper>